class Effect {
    constructor({ id, name, description, impact, duration }) {
      this.id = id;
      this.name = name;
      this.description = description;
      this.impact = impact; // 效果的屬性影響
      this.duration = duration; // 效果持續時間
    }
  }
  